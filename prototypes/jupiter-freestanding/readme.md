# JUnit 5 Prototype: Custom Jupiter Engine

That's right.  We're building our own engine.  We're going to the moon!

Run the prototype with:

```shell
$ cd prototypes/
$ ./gradlew jupiter-engine:run
```
